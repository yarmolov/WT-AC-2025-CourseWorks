# Тесты нормоконтроля для документов .docx

Автоматическая проверка соответствия документов требованиям нормоконтроля (Вариант 3: гибридный подход с python-docx + OOXML).

## Два режима работы

### 1. Режим тестирования (test_normocontrol_ooxml.py)
Тесты падают при обнаружении проблем. Используется для CI/CD.

### 2. Режим отчётов (test_normocontrol_report.py) ⭐
Тесты **не падают**, а собирают все проблемы в отчёт. Рекомендуется для студентов.

## Структура

```
tests/
├── conftest.py                   # Фикстуры pytest + система отчётов
├── test_normocontrol_ooxml.py    # Тесты (падают при ошибках)
├── test_normocontrol_report.py   # Тесты с отчётами (не падают) ⭐
├── helpers/
│   ├── __init__.py
│   ├── ooxml_utils.py            # Утилиты для работы с OOXML
│   └── report.py                 # Генератор отчётов
├── ПЗ.docx                       # Тестовые документы
├── Приложение А.docx
└── Приложение Б.docx
```

## Что проверяется

### ✅ Настройка страницы
- **Поля**: левое 30 мм, правое 10 мм, верхнее/нижнее 20 мм
- **Размер**: A4 (210×297 мм)

### ✅ Форматирование абзацев
- **Отступ первой строки**: 1.25 см (или 1.5 см)
- **Межстрочный интервал**: полуторный (1.5)
- **Выравнивание**: по ширине

### ✅ Шрифты
- **Основной шрифт**: Times New Roman
- **Размер**: 14 пт основной текст, 12 пт таблицы/сноски

### ✅ Структура документа
- Наличие обязательных разделов: Содержание, Введение, Заключение, Список источников
- Формат подписей таблиц: "Таблица X.Y — Название"

## Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуемые пакеты:
- `pytest>=8.0.0` — фреймворк для тестирования
- `python-docx>=1.1.0` — работа с .docx
- `lxml>=5.0.0` — парсинг OOXML

## Запуск тестов

### 🎯 Рекомендуемый способ: генерация отчёта
```bash
# Markdown отчёт (по умолчанию)
pytest tests/test_normocontrol_report.py

# Все форматы (Markdown + JSON + Text)
pytest tests/test_normocontrol_report.py --report-format=all

# Только JSON
pytest tests/test_normocontrol_report.py --report-format=json

# Сохранить в другую папку
pytest tests/test_normocontrol_report.py --report-dir=my_reports
```

**Результат:** 
- Все тесты проходят ✅
- Файл с детальным отчётом создаётся в папке `normocontrol_reports/`
- Показывается сводка в консоли

### Классический режим (падающие тесты)
```bash
pytest tests/ -v
```

### Классический режим (падающие тесты)
```bash
# Все проверки (упадёт при первой проблеме)
pytest tests/test_normocontrol_ooxml.py -v

### Конкретный документ
```bash
pytest tests/test_normocontrol_ooxml.py::TestPageSetup::test_page_margins[ПЗ.docx] -v
```

### Только проверка полей
```bash
pytest tests/test_normocontrol_ooxml.py::TestPageSetup -v
```

### Только структура документа
```bash
pytest tests/test_normocontrol_ooxml.py::TestDocumentStructure -v
```

### Быстрая сводная проверка
```bash
pytest tests/test_normocontrol_ooxml.py::test_normocontrol_summary -v
```

### С подробным выводом
```bash
pytest tests/ -vv --tb=short
```

## Категории тестов

| Класс | Описание | Тесты |
|-------|----------|-------|
| `TestPageSetup` | Настройка страницы | Поля, размер страницы A4 |
| `TestParagraphFormatting` | Форматирование абзацев | Отступы, интервал, выравнивание |
| `TestFonts` | Проверка шрифтов | Times New Roman, размеры 14/12 pt |
| `TestDocumentStructure` | Структура документа | Разделы, таблицы, подписи |
| `TestAdvanced` | Расширенные проверки | Отсутствие прямого форматирования (опционально) |

## Форматы отчётов

### Markdown (рекомендуется)
Красиво отформатированный отчёт с иконками и структурой.
```bash
pytest tests/test_normocontrol_report.py --report-format=markdown
```

### JSON
Структурированные данные для автоматической обработки.
```bash
pytest tests/test_normocontrol_report.py --report-format=json
```

### Text
Простой текстовый файл.
```bash
pytest tests/test_normocontrol_report.py --report-format=text
```

### Все форматы сразу
```bash
pytest tests/test_normocontrol_report.py --report-format=all
```

## Примеры использования

## Примеры использования

### Для студентов: проверка своей работы
```bash
# 1. Положить свой документ в папку tests/ (например, МояРабота.docx)
# 2. Запустить проверку с генерацией отчёта
pytest tests/test_normocontrol_report.py --report-format=markdown

# 3. Открыть отчёт:
# normocontrol_reports/normocontrol_report_YYYYMMDD_HHMMSS.md
```

### Проверить один конкретный файл
```python
pytest tests/test_normocontrol_ooxml.py -k "ПЗ.docx" -v
```

### Пропустить расширенные проверки
```python
pytest tests/test_normocontrol_ooxml.py -k "not Advanced" -v
```

### Сохранить отчёт в файл
```bash
pytest tests/ -v --html=report.html --self-contained-html
```
(требует: `pip install pytest-html`)

## Интеграция с CI/CD

### GitHub Actions
```yaml
name: Normocontrol

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: pytest tests/test_normocontrol_ooxml.py -v
```

## Добавление новых документов

1. Поместите `.docx` файл в папку `tests/`
2. Добавьте фикстуру в `conftest.py`:
   ```python
   @pytest.fixture
   def my_doc():
       return TESTS_DIR / "МойДокумент.docx"
   ```
3. Или используйте параметризованную фикстуру `any_docx` (автоматически прогоняет все документы)

## Утилиты (helpers/ooxml_utils.py)

Доступные функции:
- `load_xml(docx_path, xml_path)` — загрузка XML из .docx
- `get_document_xml(docx_path)` — получить основной XML документа
- `get_page_margins(doc_xml)` — извлечь поля страницы
- `get_paragraph_properties(p)` — свойства параграфа
- `get_run_properties(r)` — свойства run (шрифт, размер)
- `mm_to_twips(mm)`, `twips_to_mm(twips)` — конвертация единиц
- `cm_to_twips(cm)`, `twips_to_cm(twips)` — конвертация единиц
- `pt_to_half_points(pt)`, `half_points_to_pt(hp)` — конвертация размеров шрифта
- `check_margins(...)` — быстрая проверка полей

## Отладка

### Посмотреть поля документа
```python
from helpers.ooxml_utils import get_document_xml, get_page_margins, twips_to_mm
from pathlib import Path

doc_xml = get_document_xml(Path("tests/ПЗ.docx"))
margins = get_page_margins(doc_xml)

for key, value in margins.items():
    print(f"{key}: {twips_to_mm(value):.1f} мм")
```

### Посмотреть все параграфы с отступами
```python
from helpers.ooxml_utils import get_document_xml, get_paragraph_properties, twips_to_cm, NS

doc_xml = get_document_xml(Path("tests/ПЗ.docx"))
paragraphs = doc_xml.xpath(".//w:p", namespaces=NS)

for i, p in enumerate(paragraphs[:20]):
    props = get_paragraph_properties(p)
    if 'ind' in props and props['ind'].get('firstLine'):
        indent = twips_to_cm(int(props['ind']['firstLine']))
        print(f"Параграф {i}: отступ {indent:.2f} см")
```

## Известные ограничения

❌ **Не проверяется:**
- Реальная пагинация (на какой странице что находится)
- Визуальное расположение номеров страниц (требует рендер)
- Точное соответствие ГОСТ (только базовые требования)

✅ **Проверяется:**
- Все параметры, заданные в OOXML (поля, отступы, шрифты)
- Структура документа и наличие разделов
- Формат подписей таблиц и рисунков

Для более строгой проверки используйте **Вариант 4** (LibreOffice→PDF) или **Вариант 5** (Word COM).

## Полезные ссылки

- [Требования к нормоконтролю](../docs/Требования_к_нормоконтролю.md)
- [Варианты автоматизации](../docs/Варианты_автоматизации_нормоконтроля.md)
- [python-docx documentation](https://python-docx.readthedocs.io/)
- [OOXML specification](http://officeopenxml.com/anatomyofOOXML.php)
